<style>
    html {
        background-color: #2a2a2a;
    }
    .navbar {
        background-color: #1c1c1c;
    }
    .navbar-item, .navbar-item a, .section .title, .section .subtitle, .section p, small, .panel-tabs a, .panel-tabs a.is-active, a.panel-block.is-active {
        color: white;
    }
    .navbar.has-shadow {
        box-shadow: 0 2px 0 0 #101010;
    }
    code {
        background-color: #1c1c1c;
    }
    div.login-container {
        background-color: #1c1c1c;
        color: white;
    }
    label.label {
        color: white;
    }
    label.checkbox {
        color: white;
    }
    label.checkbox:hover {
        color: #bbbbbb;
    }
    .panel-tabs, .panel-heading, div#users {
        background-color: #1c1c1c;
        color: white;
    }
    a.panel-block:hover {
        background-color: #2a2a2a;
    }
    span.panel-icon svg {
        fill: white;
        color: white;
    }
    p.panel-tabs a:hover {
        background-color: #2a2a2a;
    }
    .panel-tabs a.is-active {
        border-bottom-color: #276CDA;
    }

    div.buttons.has-addons.filter button.button {
        color: white;
        background-color: #1c1c1c;
        border-color: #1c1c1c;
    }
    div.buttons.has-addons.filter button:hover{
        color: white;
        background-color: #2a2a2a;
        border-color: #1c1c1c;
    }
    div.buttons.has-addons.filter button.button.is-active {
        background-color: #2a2a2a;
    }
    div.level-item h1 {
        color: white;
    }
    div.level-item button {
        color: white;
        background-color: #1c1c1c;
        border-color: #1c1c1c;
    }
    div.level-item button:hover {
        color: white;
        background-color: #2a2a2a;
        border-color: #1c1c1c;
    }
    div.columns h1, h2, p, span, .title, .subtitle {
        color: white;
    }
    div#links table.table thead tr th {
        color: white;
        background-color: #1c1c1c;
        border-color: #1c1c1c;
    }
    div#links table.table tbody tr td {
        color: white;
        background-color: #1c1c1c;
        border-color: #1c1c1c;
    }
    div.box {
        background-color: #1c1c1c;
    }
    div.box button {
        color: white;
        background-color: #2a2a2a;
        border-color: #2a2a2a;
    }
</style>